#getting input from user
def calculate_height(h0, t):
    g=9.8
    return h0 - 0.5 * g* t* t

height = float(input("Enter intial height"))
time = float(input("Enter time"))
calculate_height(height, time)
print(f"Height of the ball at {time} is {calculate_height(height, time)}")

height = float(input("Enter intial height"))
time = float(input("Enter time"))
calculate_height(height, time)
print(f"Height of the ball at {time} is {calculate_height(height, time)}")

height = float(input("Enter intial height"))
time = float(input("Enter time"))
calculate_height(height, time)
print(f"Height of the ball at {time} is {calculate_height(height, time)}")
     


   
def calculate_car_distance(t):
    speed=20
    return speed*t
time = float(input("Enter time"))
calculate_car_distance(time)
print(f"The car will travel {calculate_car_distance(time)} in {time}")

def calculate_car_distance(t):
    speed=20
    return speed*t
time = float(input("Enter time"))
print(f"The car will travel {calculate_car_distance(time)} in {time}")

def calculate_car_distance(t):
    speed=20
    return speed*t
time = float(input("Enter time"))
print(f"The car will travel {calculate_car_distance(time)} in {time}")